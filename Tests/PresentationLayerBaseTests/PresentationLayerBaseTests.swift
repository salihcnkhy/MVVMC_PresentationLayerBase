import XCTest
@testable import PresentationLayerBase

final class PresentationLayerBaseTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(PresentationLayerBase().text, "Hello, World!")
    }
}
